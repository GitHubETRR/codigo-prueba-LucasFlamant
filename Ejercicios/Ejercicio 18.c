#include <stdio.h>

int main()
{
   
    float radio;
    float area;
    PI = 3.14
    printf("Ingrese el valor de radio de su circulo para emitir el valor del área\n");
    scanf("%f",&radio);
    area = PI * radio * 2
    printf("El area de su circulo es %f", area);

    return 0;
}